install vagrant:
       sudo apt-get install vagrant
install virtualbox
        sudo apt-get install virtualbox
we have to crate a folder and change our location to that folder
then we have to do:
      vagrant init hashicorp/precise64
      vagrant up
      vagrant ssh
